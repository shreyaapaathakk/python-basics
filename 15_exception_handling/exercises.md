# Exception Handling Exercises

## Exercise 1

Write a program that catches a `ValueError` when the user enters invalid input.

---

## Exercise 2

Handle a `ZeroDivisionError`.

---

## Exercise 3

Handle both `ValueError` and `ZeroDivisionError` in one program.

---

## Exercise 4

Use an `else` block to display the result only if no exception occurs.

---

## Exercise 5

Use a `finally` block to display a completion message.

---

## Exercise 6

Raise a `ValueError` if a number is negative.

---

## Exercise 7

Create a custom exception named `InvalidAgeError`.

---

## Exercise 8

Handle a `FileNotFoundError` while opening a file.

---

# Bonus Challenge

Create a calculator program that repeatedly asks the user for two numbers and an operation (`+`, `-`, `*`, `/`). Handle invalid numbers, division by zero, and invalid operations without crashing.
