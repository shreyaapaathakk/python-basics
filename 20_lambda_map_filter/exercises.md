# Exercises

## Exercise 1

Create a lambda function that returns the cube of a number.

---

## Exercise 2

Use `map()` to double every number in a list.

---

## Exercise 3

Use `filter()` to select numbers greater than 10.

---

## Exercise 4

Use `reduce()` to find the product of a list of numbers.

---

## Exercise 5

Sort a list of names by their length using `lambda`.

---

## Exercise 6

Sort a list of tuples by the second element.

---

## Exercise 7

Use `map()` with a lambda function to convert temperatures from Celsius to Fahrenheit.

---

# Bonus Challenge

Create a program that filters passing students (marks ≥ 50), increases their marks by 5 using `map()`, and calculates the total marks using `reduce()`.
